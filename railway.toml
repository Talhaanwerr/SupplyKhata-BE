[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Mirrors Dockerfile CMD — Nest emits dist/src/main.js
startCommand = "node dist/src/main"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
